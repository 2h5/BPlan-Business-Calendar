/**
 * Shared geometry for things that float over a tab's content, so the undo
 * toast and the add button stay clear of the native tab bar and of each other.
 */

/** Clears the floating native tab bar, which sits above the home indicator. */
export const TAB_BAR_CLEARANCE = 64;

/** The undo toast's height, which the add button rises by while one shows. */
export const TOAST_HEIGHT = 48;

/** The add button's diameter. */
export const FAB_SIZE = 56;
