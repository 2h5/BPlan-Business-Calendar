/**
 * The product's user-facing name, in one place so every screen says the same
 * thing. Internal identifiers — the `@cal/*` packages, the bundle id, the URL
 * scheme — are deliberately separate and do not follow it.
 */
export const APP_NAME = 'BPlan';

/** The paid plan, as it is sold and mentioned everywhere in the app. */
export const PRO_PLAN_NAME = `${APP_NAME} Pro`;
